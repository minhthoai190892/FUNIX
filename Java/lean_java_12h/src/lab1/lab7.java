package lab1;

import java.util.Scanner;

public class lab7 {

    public static void main(String[] args) {
        System.out.println(
                "==== 7.1. Bài tập về hàm trong Java: viết hàm hiển thị ra màn hình các số chia hết cho 3 và không chia hết cho 5 ====");
        /*
         * Task
         * Write a method that accepts an array arr of integers from the user and
         * displays numbers, which are divisible by 3 but not divisible by 5 in arr, on
         * the screen.
         */
        Scanner scanner = new Scanner(System.in);
        // System.out.print("Enter n: ");
        // int n = scanner.nextInt();
        // scanner.nextLine();
        // int[] array = new int[n];
        // for (int i = 0; i < n; i++) {
        // array[i] = scanner.nextInt();
        // }
        // show(array);
        System.out.println("==== 7.2. Hàm có tham số trong Java ====");
        /*
         * Task
         * Write a method to print strings of length greater than 3 in an array arr.
         */
        // System.out.print("Enter n: ");
        // int n = scanner.nextInt();
        // scanner.nextLine();
        // String[] array = new String[n];
        // for (int i = 0; i < n; i++) {
        // array[i] = scanner.nextLine();
        // }
        // showStringArray(array);
        System.out.println("==== 7.3. Mảng (array) trong Java ====");
        /*
         * Task
         * Write a program that accepts 10 integers from the user and prints those
         * entered numbers on the screen.
         */
        // System.out.print("Enter n: ");
        // int n = scanner.nextInt();
        // scanner.nextLine();
        // int[] array = new int[n];
        // for (int i = 0; i < n; i++) {
        // array[i] = scanner.nextInt();
        // }
        // showIntegerArray(array);
        System.out.println("==== 7.4. Bài tập về mảng trong Java: tìm phần tử lớn nhất trong mảng ====");
        /*
         * Task
         * Write a program that accepts an array arr of n integers from the user and
         * prints the largest element in the array.
         * 
         * For example, if n = 5, arr = [2, 7, 6, 8, 9], enter the values as below:
         */
        // System.out.print("Enter n: ");
        // int n = scanner.nextInt();
        // scanner.nextLine();
        // int[] array = new int[n];
        // for (int i = 0; i < n; i++) {
        // array[i] = scanner.nextInt();
        // }
        // showMaxValue(array);
        System.out.println(
                "==== 7.5. Bài tập về mảng trong Java: tính tổng của phần tử đầu tiên và cuối cùng trong mảng ====");
        /*
         * Task
         * Write a program that accepts an array arr of n integers from the user and
         * prints the sum of the first element and the last element in arr on the
         * screen.
         * 
         * For example, if n = 5, arr = [2, 7, 6, 8, 9], enter the following values:
         */
        // System.out.print("Enter n: ");
        // int n = scanner.nextInt();
        // scanner.nextLine();
        // int[] array = new int[n];
        // for (int i = 0; i < n; i++) {
        // array[i] = scanner.nextInt();
        // }
        // System.out.println("Sum is " + (array[0] + array[n - 1]));
        // SumFristAndLassArray(array, n);
        System.out.println("==== 7.6. Bài tập về mảng trong Java: hiển thị ra màn hình các số chẵn trong mảng ====");
        /*
         * Task
         * Write a program that accepts an array arr of n integers from the user and
         * prints all even numbers in arr.
         */
        // System.out.print("Enter n: ");
        // int n = scanner.nextInt();
        // scanner.nextLine();
        // int[] array = new int[n];
        // for (int i = 0; i < n; i++) {
        // array[i] = scanner.nextInt();
        // }
        // showEven(array);
        System.out.println("==== 7.7. Bài tập Java cơ bản: đếm số phần tử có giá trị bằng k trong mảng ====");
        /*
         * Task
         * Given an array arr of n integers and an integer k. Write a program that
         * accepts these variables from the user and prints the number of elements in
         * arr, which are equal to k.
         * 
         * For example, if n = 6, arr = [3, 8, 7, 8, 3, 3], k = 3, enter the following
         * values:
         */
        // System.out.print("Enter n: ");
        // int n = scanner.nextInt();
        // int[] array = new int[n];
        // scanner.nextLine();
        // for (int i = 0; i < n; i++) {
        // array[i] = scanner.nextInt();
        // }
        // System.out.println("Enter k: ");
        // int k = scanner.nextInt();
        // countArray(array, k);

        System.out.println("==== 7.8. Bài tập Java cơ bản: tính tổng các phần tử có giá trị lớn hơn 0 trong mảng ====");

        /*
         * Task
         * Write a program that accepts an array arr of n integers from the user and
         * prints the sum of all odd numbers which are greater than 0 in the array.
         */
        // System.out.print("Enter n: ");
        // int n = scanner.nextInt();
        // int[] array = new int[n];
        // for (int i = 0; i < n; i++) {
        // array[i] = scanner.nextInt();
        // }
        // sumOddOfArray(array);
        System.out
                .println("==== 7.9. Bài tập Java cơ bản: hiển thị các phần tử nằm trong đoạn [0, 10] trong mảng ====");
        /*
         * Task
         * Write a program that accepts an array arr of n integers from the user and
         * prints all numbers which are greater than or equal to 0 and less than or
         * equal to 10 in arr.
         */
        // System.out.print("Enter n: ");
        // int n = scanner.nextInt();
        // int[] array = new int[n];
        // for (int i = 0; i < n; i++) {
        // array[i] = scanner.nextInt();
        // }
        // showArray(array);
        System.out.println("==== 7.10. Bài tập Java cơ bản: sắp xếp mảng theo thứ tự tăng dần ====");
        /*
         * Task
         * Write a program that accepts an array arr of n integers from the user and
         * sorts all elements in ascending order and print the sorted array on the
         * screen.
         * 
         * For example, if n = 6, arr = [5,3,2,6,7,7], enter the following values:
         */
        // System.out.print("Enter n: ");
        // int n = scanner.nextInt();
        // int[] array = new int[n];
        // for (int i = 0; i < n; i++) {
        // array[i] = scanner.nextInt();
        // }
        // ascendingArray(array, n);
        System.out.println("==== 7.11. Mảng 2 chiều trong Java ====");
        /*
         * Task
         * Given a 2-dimensional array arr (n rows, m columns). Write a program that
         * accepts arr from the user and calculates the sum of all elements in arr.
         * 
         * For example, if n = 2, m = 3, arr = [[5, 7, 3], [1, 2, 4]]
         */
        // System.out.print("Entter n ");
        // int n = scanner.nextInt();
        // scanner.nextLine();
        // System.out.print("Entter m ");
        // int m = scanner.nextInt();
        // scanner.nextLine();
        // int[][] array = new int[n][m];
        // for (int i = 0; i < n; i++) {
        // for (int j = 0; j < m; j++) {
        // array[i][j] = scanner.nextInt();
        // }
        // }

        // twoDimensional(array, n, m);
        System.out.println("==== 7.12. Bài tập về mảng 2 chiều trong Java ====");
        /*
         * Task
         * Given a two-dimensional array arr (n rows, m columns) of integers. Write a
         * program that accepts arr from the user and calculates the sum of numbers in
         * arr, which are divisible by 5.
         * 
         * For example, if n = 3, m = 4, arr = [[10, 4, 8, 2], [5, 6, 4, 9], [20, 3, 8,
         * 1]], enter the following values:
         */
        // String[] cars = { "Volvo", "BMW", "Ford", "Mazda" };
        // for (String i : cars) {
        //     System.out.println(i);
        // }

        String[][] car = {{"001","HN"},{"002","HasdasdN"},{"003","HasdasdN"},{"004","HasdqweN"}};
        
        System.out.println("====  ====");
        System.out.println("====  ====");

        scanner.close();
    }
    // System.out.println("==== 7.1. Bài tập về hàm trong Java: viết hàm hiển thị ra
    // màn hình các số chia hết cho 3 và không chia hết cho 5 ====");

    /**
     * 
     * @param array nhận một array
     */
    public static void show(int[] array) {
        for (int i = 0; i < array.length; i++) {
            if (array[i] % 3 == 0 && array[i] % 5 != 0) {
                System.out.print(array[i] + " ");
            }
        }
    }

    // System.out.println("==== 7.2. Hàm có tham số trong Java ====");
    public static void showStringArray(String[] array) {
        for (int i = 0; i < array.length; i++) {
            if (array[i].length() >= 3) {
                System.out.print(array[i] + " ");
            }
        }
    }

    // System.out.println("==== 7.3. Mảng (array) trong Java ====");
    public static void showIntegerArray(int[] array) {
        for (int i = 0; i < array.length; i++) {
            System.out.print(array[i] + " ");
        }
    }

    // System.out.println("==== 7.4. Bài tập về mảng trong Java: tìm phần tử lớn
    // nhất trong mảng ====");
    public static void showMaxValue(int[] array) {
        int maxValue = array[0];
        for (int i = 0; i < array.length; i++) {
            if (array[i] > maxValue) {
                maxValue = array[i];
            }
        }
        System.out.println("Max value is " + maxValue);
    }

    // System.out.println("==== 7.5. Bài tập về mảng trong Java: tính tổng của phần
    // tử đầu tiên và cuối cùng trong mảng ====");
    public static void SumFristAndLassArray(int[] array, int n) {
        int sum = 0;
        for (int i = 0; i < array.length; i++) {
            sum = array[0] + array[n - 1];
        }
        System.out.println("Sum is " + sum);
    }

    // System.out.println("==== 7.6. Bài tập về mảng trong Java: hiển thị ra màn
    // hình các số chẵn trong mảng ====");
    public static void showEven(int[] array) {
        for (int i = 0; i < array.length; i++) {
            if (array[i] % 2 == 0) {
                System.out.print(array[i] + " ");
            }
        }
    }

    // System.out.println("==== 7.7. Bài tập Java cơ bản: đếm số phần tử có giá trị
    // bằng k trong mảng ====");
    public static void countArray(int[] array, int k) {
        int count = 0;
        for (int i = 0; i < array.length; i++) {
            if (array[i] == k) {
                count++;
            }
        }
        System.out.println(count);
    }

    // System.out.println("==== 7.8. Bài tập Java cơ bản: tính tổng các phần tử có
    // giá trị lớn hơn 0 trong mảng ====");
    public static void sumOddOfArray(int[] array) {
        int count = 0;
        for (int i = 0; i < array.length; i++) {
            if (array[i] % 2 == 1 && array[i] > 0) {
                count += array[i];
            }
        }
        System.out.println("sum Odd is " + count);
    }

    // System.out.println("==== 7.9. Bài tập Java cơ bản: hiển thị các phần tử nằm
    // trong đoạn [0, 10] trong mảng ====");
    public static void showArray(int[] array) {
        for (int i = 0; i < array.length; i++) {
            if (array[i] >= 0 && array[i] <= 10) {
                System.out.print(array[i] + " ");
            }
        }
    }

    // System.out.println("==== 7.10. Bài tập Java cơ bản: sắp xếp mảng theo thứ tự
    // tăng dần ====");
    public static void ascendingArray(int[] array, int n) {
        for (int i = 0; i < n; i++) {
            for (int j = i; j < n; j++) {
                if (array[i] > array[j]) {
                    int temp;
                    temp = array[i];
                    array[i] = array[j];
                    array[j] = temp;
                }
            }
        }
        for (int i = 0; i < n; i++) {
            System.out.print(array[i] + " ");
        }
    }

    // System.out.println("==== 7.11. Mảng 2 chiều trong Java ====");
    public static void twoDimensional(int[][] array, int n, int m) {
        int sum = 0;
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < m; j++) {
                if (array[i][j] % 5 == 0) {

                    sum += array[i][j];
                }
            }
        }
        System.out.print("Sum is " + sum);
    }
    // System.out.println("==== 7.12. Bài tập về mảng 2 chiều trong Java ====");

}
